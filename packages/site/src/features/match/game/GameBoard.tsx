import * as React from "react";
import { Box } from "@chakra-ui/react";
import { Dimensions2d } from "@project/shared";

interface Props {
  size: Dimensions2d;
}

export const GameBoard: React.FC<Props> = ({}) => {
  return <Box>hello GameBoard</Box>;
};
