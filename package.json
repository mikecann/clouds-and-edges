{
  "private": true,
  "name": "tinkering-with-durable-objects",
  "version": "1.0.0",
  "workspaces": [
    "site",
    "server",
    "site-worker"
  ],
  "description": "Tinkering with some durable objects",
  "module": "./dist/index.mjs",
  "scripts": {
    "build": "yarn workspaces foreach -p -i run build",
    "deploy": "yarn workspaces foreach -p -i run deploy",
    "test": "yarn workspaces foreach -p -i run test"
  },
  "author": "mike.cann@gmail.com",
  "license": "MIT"
}
