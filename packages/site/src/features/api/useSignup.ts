export const useSignup = () => {
  return {} as any;
  // const [, setState] = useAppState();
  // return useMutation(
  //   ({ name }: { name: string }) =>
  //     apiMutate("auth.signup", {
  //       name,
  //     }),
  //   {
  //     onSuccess: async ({ userId }) => {
  //       setState((p) => ({ ...p, userId }));
  //     },
  //   }
  // );
};
